import StudentDashboard from "@/components/StudentDashboard/StudentDashboard";

const page = () => {
  return (
    <div className="mt-[4.4rem]">
      <StudentDashboard />
    </div>
  );
};

export default page;
